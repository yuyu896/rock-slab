# 提案：部署移动端扫码终端发布（2026-09-17）

## Why

移动端扫码化改造系列（扫码终端、标签 V1–V3、导出图片通道、PWA、安装向导）已全部本地验证完毕并由用户真机验收通过，需部署生产供分公司使用。**用户当次明确授权执行部署**（覆盖"改动只落本地"默认规则），并要求：本地数据不上传、生产数据保持原样。

## What Changes

- 生产环境（qhpanpan.top）执行标准发布流程：git pull → 前端构建 → collectstatic → nginx reload
- **部署内容为纯前端**（`d85301d..d321e27` 等提交：扫码终端/PWA/安装向导/标签系列），后端零改动、零迁移
- **数据不动**：deploy 仅走代码通道，无任何数据同步；本地测试数据（22 台实例、小熊供应商、测试盘点任务）留存本地库，生产业务数据原样保留

## Capabilities

### New Capabilities

（无——发布既有已验收功能）

### Modified Capabilities

（无）

## Impact

- 生产：rock-slab-nginx（前端 dist 更新）、静态资源；rock-slab-backend 仅重启无代码变化
- 前置动作：**PostgreSQL 全量备份**（部署前），异常即回滚（备份恢复 + git 回退）
- 验收：线上 `/install` 可达、移动端扫码终端可用、生产实例数据无测试数据混入
