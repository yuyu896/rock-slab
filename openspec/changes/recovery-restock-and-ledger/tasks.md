# 回收重塑 — 实施任务

## 1. 后端：去向改直接入库

- [x] 1.1 `Transfer.回收去向` 枚举：recycle_bin 改义 restock（重新入库）；`ledger.apply_document` recovery 分支目标列 在库（不再动回收库列）；`instances.py` 回收迁移 → 在库
- [x] 1.2 领用来源收口：携带 recycle_bin → 400（前后端校验与常量）；盘点 stock_bin=recycle → 400，创建页下拉删选项
- [x] 1.3 后端测试：重新入库在用→在库（台账+实例，回收库列不动）；dispose 不变；领用来源 recycle_bin 拒绝；盘点 recycle 拒绝

## 2. 后端：回收台账 + 存量归一

- [x] 2.1 `recovery-ledger` 只读 action：dispose×生效明细行展开（实例逐台/数量行），筛选（分公司/日期/品目）+ 分页 + Excel 导出
- [x] 2.2 `normalize_recycle_bin` 管理命令：预览/确认，实例回收库→在库（服务层）+ 台账列搬家（调整单留痕），幂等
- [x] 2.3 后端测试：台账逐台展开/数量行/未生效不入/筛选导出；归一命令幂等与留痕

## 3. 前端

- [x] 3.1 回收创建页：去向文案「重新入库（在用→在库）」/「直接处置」，删回收库选项；领用创建页来源下拉删「回收库」
- [x] 3.2 盘点创建页库别删「回收库」
- [x] 3.3 新页 `RecoveryLedger.vue` + 路由 + 侧边栏（回收组两入口）+ 筛选/分页/导出；vitest（行展开与入口渲染）

## 4. 验证与收尾

- [x] 4.1 全量 pytest（含对账/架构）；`npm run build` + vitest 全绿
- [x] 4.2 本地手验：回收重新入库（在用→在库）→ 领用只余新品库 → 直接处置 → 回收台账逐台流水/筛选/导出；归一命令本地演练
