## 1. 纠错命令实现

- [x] 1.1 新建 `backend/apps/assets/management/commands/prod_data_fix_xm25.py`：常量固化目标，`--apply` 开关，单事务（建账侧 + 未生效单据侧）
- [x] 1.2 建账侧：调整单 + 台账行成对删；未生效侧：全部非生效单据直删（零回退）+ 关联通知清理
- [x] 1.3 前置断言：无生效单据（from/to）、无实例档案；幂等跳过；`--apply` 末尾内嵌对账复验

## 2. 测试

- [x] 2.1 功能：建账+待审批单全清、生效单据零回退验证、员工节点保留、对账零差异
- [x] 2.2 边界：存在生效单据拒绝零变更、存在实例拒绝；dry-run 不写库；幂等重跑
- [x] 2.3 后端全量 pytest 通过（703 passed）

## 3. 生产执行

- [x] 3.1 部署新镜像；`/root/backup_db.sh` 即时备份（rock_slab_20260915_163506.sql.gz）
- [x] 3.2 dry-run 存档复核（117 调整单+117 行；42 张待审批单号）
- [x] 3.3 `--apply` + 内嵌对账；独立复跑 `check_ledger_consistency` exit=0（4113 行零差异，924 条通知清理）
- [x] 3.4 抽查：台账 0、待审批采购单 0、员工 2 人在；幂等复证
