## 1. 资产导入模板精简

- [x] 1.1 `download_template`（资产）表头去掉 电脑序列号、图片、是否租用、是否充足、使用人（保留 15 列）
- [x] 1.2 测试：模板不含这 5 列（含既已移除的序号/警戒线/分公司编号 一并断言）

## 2. 固定资产导入模板精简

- [x] 2.1 新增 `FA_TEMPLATE_HEADERS = [资产编号, 电脑序列号, 供应商, 入库日期, 所属部门, 使用人, 当前状态]`，`download_template` 改用它；移除 `FA_READONLY_COLS` 与「自动继承」批注逻辑（`FA_HEADERS` 保留供导出 19 列用）
- [x] 2.2 测试：模板仅这 7 列、无序号、无自动继承批注

## 3. 验证

- [x] 3.1 后端 `pytest` 全绿（370 passed）
- [x] 3.2 Django check 干净；OpenSpec validate 通过
- [ ] 3.3 本地手动：下载两个模板，确认表头已精简（资产去 5 列；固定资产仅 7 列、无序号、无自动继承标注）
