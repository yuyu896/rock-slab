# 行级供应商 + 批量操作 — 实施任务

## 1. 后端：行级供应商

- [x] 1.1 `TransferLine.供应商` 可选列（migration）+ 入参序列化（仅 purchase 行，其他类型拒绝）+ 导入解析行级供应商
- [x] 1.2 实例供应商派生改 出生行供应商→单头 依次回退；采购详情行表/导出显示行级（空回退）
- [x] 1.3 测试：一单两供应商（行级落库+实例分别显示）、行空回退单头、非采购行携带被拒、导入行级

## 2. 后端：batch-update 端点

- [x] 2.1 `POST /fixed-assets/batch-update`（白名单 供应商/备注/序列号；出生行维护；序列号逐台唯一校验；逐台 results/errors）
- [x] 2.2 测试：批量供应商/备注/序列号一一对应/重复跳过/非白名单拒/无权限拒

## 3. 前端

- [x] 3.1 TransferLinesEditor 采购行加供应商输入；采购详情行表加供应商列（回退显示）
- [x] 3.2 FixedAssetList 勾选后「批量操作」菜单（打印/供应商/备注/序列号）+ 三个弹窗（序列号逐台扫码）+ api 函数
- [x] 3.3 vitest：行级供应商输入入参、批量菜单渲染与权限、batch-update 调用

## 4. 验证与收尾

- [x] 4.1 全量 pytest + build/vitest
- [x] 4.2 本地手验：一单两供应商建单审批→实例分别显示；批量改供应商/备注；扫码补序列号
- [ ] 4.3 生产部署（待部署）
