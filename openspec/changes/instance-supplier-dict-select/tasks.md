## 1. 前端收口

- [x] 1.1 `FixedAssetList.vue` 行编辑弹窗：供应商自由输入改字典下拉（getSuppliers），首项「继承出生单」（值空）；空字典显示占位不阻断
- [x] 1.2 批量修改供应商弹窗：同款下拉 + 「设为继承」选项；确认文案明示影响行数与"回落出生单（可能为空）"
- [x] 1.3 前端用例：下拉选项来源、选择落值、继承项清空覆盖、批量确认文案

## 2. 存量归一（用户已确认，2026-09-22）

- [x] 2.1 `prod_data_fix` 惯例管理命令：实例覆盖值别名映射归一（小熊/小熊u租/小熊U组→小熊U租 等保守映射表），默认 dry-run 输出影响行数，`--apply` 执行
- [x] 2.2 命令测试：映射命中/未命中不动/dry-run 零写入

## 3. 验证收尾

- [x] 3.1 vitest 全量 + `npm run build` 通过；（若做归一）后端 pytest 全量
- [x] 3.2 拆 feat + openspec 两 commit，push
- [ ] 3.3 手验：行编辑/批量均为下拉且可继承；存量展示不变
- [ ] 3.4 生产部署（用户自行）；归一脚本 dry-run 报告用户核对后自行 --apply（若启用）
