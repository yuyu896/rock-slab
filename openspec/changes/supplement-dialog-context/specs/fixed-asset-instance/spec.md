# fixed-asset-instance 增量

## ADDED Requirements

### Requirement: 补录弹窗上下文信息

序列号补录弹窗 MUST 展示实例的识别上下文：内部编号、**品目名称、使用人、部门**（只读，空值显示 -）——供补录人核对设备归属后再录入序列号。输入字段 SHALL 维持仅 序列号/备注。

#### Scenario: 弹窗展示上下文

- **WHEN** 用户点击某实例「补录序列号」
- **THEN** 弹窗显示该实例的内部编号、品目名称、使用人、部门，随后为序列号/备注输入框
