## 1. 前端

- [ ] 1.1 `PurchaseCreate.vue`：引入 useUserStore，form 初始 `采购经办人: userStore.profile?.name || ''`
- [ ] 1.2 vitest 全量 + `npm run build` 通过（确认无「初始为空」依赖断言）

## 2. 收尾

- [ ] 2.1 拆 feat + openspec 两 commit，push
- [ ] 2.2 手验：打开创建页见自己姓名；改/清空提交均正常
