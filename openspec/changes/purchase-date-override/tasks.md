## 1. 模型与取数链

- [x] 1.1 `FixedAsset` 新增 `采购日期 = DateField('采购日期（个体覆盖，空=批次口径）', null=True, blank=True)`（置于 规格 之后）；migration 0028
- [x] 1.2 序列化器 `get_采购日期` 改两级链：个体覆盖 → 出生单日期（无出生行且无覆盖返回 None）
- [x] 1.3 timeline birth 节点 `采购日期` 走同链；单据 `日期` 保持单据原值

## 2. 数据应用（泉州二分 7 台）

- [x] 2.1 新增 `prod_data_fix_qz2_purchase_dates`：常量表 (编号→date)，断言全存在/属泉州二分，dry-run/apply/幂等，仅写覆盖列
- [x] 2.2 测试：序列化器链（覆盖生效/空回落/无出生行）、timeline 链与单据日期分离、填充命令（填值/幂等/缺失中止）
- [x] 2.3 后端全量 pytest 通过（738 passed）

## 3. 部署与生产应用

- [x] 3.1 部署（migrate 0028 上列 + 对账/授权双门禁绿）；备份 rock_slab_20260918_134815.sql.gz
- [x] 3.2 填充命令 dry-run 复核（7 条 旧派生值 → 新覆盖值）
- [x] 3.3 `--apply` 写入 7 条 → 序列化器抽查 7/7 OK、全系统 6758 实例仅 7 个覆盖有值（6751 个空=零波及）、幂等复证 0 条、对账零差异
