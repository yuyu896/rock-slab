## 1. 纠错命令实现

- [x] 1.1 新建 `backend/apps/assets/management/commands/prod_data_fix_hz2.py`：常量固化目标，`--apply` 开关；单事务合体（实例侧 + 建账侧）
- [x] 1.2 实例侧：出生单收集断言（purchase+生效+纯实例行+镜像三列）→ 删链接/实例/单据 → `_rollback_ledger` 实时算量回退 → 通知清理；发号序列保留
- [x] 1.3 建账侧：非实例品目调整单 + 台账行成对删（断言非实例品目无流转单据行、无错挂实例）
- [x] 1.4 幂等跳过；`--apply` 末尾内嵌对账复验

## 2. 测试

- [x] 2.1 功能：合体整清后单/实例/台账全归零、员工与节点保留、对账零差异
- [x] 2.2 边界：镜像不一致拒绝、非实例品目挂实例拒绝，均零变更；dry-run 不写库；幂等重跑
- [x] 2.3 后端全量 pytest 通过（699 passed）

## 3. 生产执行

- [ ] 3.1 部署新镜像；`/root/backup_db.sh` 即时备份
- [ ] 3.2 dry-run 存档复核（1 单 88 实例 −41/−47；40 调整单 + 40 行）
- [ ] 3.3 `--apply` + 内嵌对账；独立复跑 `check_ledger_consistency` exit=0
- [ ] 3.4 抽查：实例 0、台账行 0、员工余晓倩在；幂等复证
