# 实例级供应商覆盖 — 实施任务

- [x] 1.1 FixedAsset 加 `供应商` 可选列（migration）；get_供应商 三级派生（实例→出生行→单头）
- [x] 1.2 batch-update 供应商分支改写实例字段（不触碰出生行；无出生行不再跳过）；测试：勾选 1 台同行 3 台仅 1 台变、三级派生、覆盖优先
- [x] 1.3 全量 pytest 728 passed；生产部署完成（migration 0026 应用、字段线上验证、对账零差异）
