## 1. 纠错命令实现

- [x] 1.1 新建 `backend/apps/assets/management/commands/prod_data_fix_nb33.py`：常量固化 3 个内部编号，`--apply` 开关，单事务
- [x] 1.2 点删逻辑：按出生行分组 → 行数量减去该行被删数 → 删关联/实例 → 台账同额直减（断言非负）；发号序列保留
- [x] 1.3 前置断言：存在/本分公司/在库/生效采购单/行账一致/镜像一致；幂等跳过；末尾内嵌对账复验

## 2. 测试

- [x] 2.1 功能：多实例行点删后行数量/台账/实例三处同步、同行其余实例与他分公司零波及、对账零差异
- [x] 2.2 边界：非在库拒绝、行账错位拒绝、目标缺失报错；dry-run 不写库；幂等重跑
- [x] 2.3 后端全量 pytest 通过（708 passed）

## 3. 生产执行

- [x] 3.1 部署新镜像；`/root/backup_db.sh` 即时备份（rock_slab_20260915_172425.sql.gz）
- [x] 3.2 dry-run 存档复核（3 编号、两行 6→4/13→12、在库 75→72）
- [x] 3.3 `--apply` + 内嵌对账；独立复跑 `check_ledger_consistency` exit=0（4561 行零差异）
- [x] 3.4 抽查：3 编号已删、72 实例镜像 72 台账、他分公司同品目 2277（导入增长中零误伤）；幂等复证全跳过
