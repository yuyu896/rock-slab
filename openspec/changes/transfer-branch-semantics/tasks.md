# 单据分公司语义化 — 实施任务

## 1. 模型与调用点

- [x] 1.1 `Transfer.build` 语义化构造器（单公司类型所属分公司/调拨双参；非法参数组合拒绝）+ `业务分公司`/`业务分公司名` 属性
- [x] 1.2 `_create_action` 与 `import_excel` 改走 build；全量回归既有单据测试（5 类建单方向不变）
- [x] 1.3 架构测试：业务代码禁 from/to 直赋（白名单 models/migrations/tests）

## 2. 验证

- [x] 2.1 全量 pytest（含新构造器/属性/架构测试）
- [x] 2.2 build +1 组单测：三类参数映射与非法组合拒绝
