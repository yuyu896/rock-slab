## 1. 详情头部按钮垂直居中

- [x] 1.1 将 `.detail-header` 的 `align-items` 从 `flex-start` 改为 `center`

## 2. 角色标签与姓名左对齐

- [x] 2.1 为 `.profile-info` 添加 `align-items: flex-start`
- [ ] 2.2 将 `.profile-role` 的 `padding` 从 `4px 10px` 改为 `2px 0`，移除 `border-radius`，使角色文字与姓名文字精确左对齐
