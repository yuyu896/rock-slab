## 1. FixedAsset 模型补字段 + 唯一约束

- [x] 1.1 `FixedAsset` 新增字段：物品分类、规格、是否租用、数量、单价、购入金额、出库日期
- [x] 1.2 `UniqueConstraint(fields=['分公司','序列号'], condition=~Q(序列号=''))`（同分公司内序列号唯一、空序列号不约束）
- [x] 1.3 迁移已生成（0006）

## 2. 资产导入模板（15 列）

- [x] 2.1 `download_template`（资产）表头改为：分公司、资产编号、资产类目、物品分类、资产名称、入库日期、是否租用、数量、规格、单价、购入金额、出库日期、所属部门、当前状态、备注
- [x] 2.2 测试：模板恰好这 15 列（含是否租用、不含供应商）

## 3. 固定资产导入模板（18 列）+ 导入读新列

- [x] 3.1 `FA_TEMPLATE_HEADERS` 改为 18 列
- [x] 3.2 固定资产 `import_excel`：按列读取全部 18 列（含新字段）写入 FixedAsset；分公司/分公司编号/branch 取自文件
- [x] 3.3 测试：模板恰好 18 列

## 4. 表内去重

- [x] 4.1 资产 `import_excel`：seen={(分公司,资产编号)}，重复 → 提醒「资产编号 X 重复」（先于 DB 检查）
- [x] 4.2 固定资产 `import_excel`：seen={(分公司,电脑序列号)}（序列号非空），重复 → 提醒「电脑序列号 X 重复」
- [x] 4.3 测试：同表内重复资产编号/电脑序列号被拒并提醒（2 用例）

## 5. 验证

- [x] 5.1 后端 `pytest` 全绿（372 passed）
- [x] 5.2 Django check 干净 / 迁移无漂移 / OpenSpec validate 通过
- [ ] 5.3 部署前：核查线上是否已有「同分公司+同序列号(非空)」重复，若有先清理（否则建唯一约束迁移会失败）
- [ ] 5.4 本地手动：下载两模板确认列；导入含重复行确认去重提醒
