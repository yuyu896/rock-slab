## 1. 修复领用出库（AssignList.vue）

- [ ] 1.1 表单字段重命名：`数量` → `调拨数量`，`使用人` → 合适字段（如 `调拨原因`）
- [ ] 1.2 添加 `调拨日期` 必填字段到表单
- [ ] 1.3 提交时传递 `toBranch`（FK）字段
- [ ] 1.4 确保所有空字符串字段不会触发 CharField 验证错误

## 2. 修复调拨（TransferList.vue）

- [ ] 2.1 表单字段重命名：`数量` → `调拨数量`
- [ ] 2.2 添加 `调拨日期` 必填字段到表单
- [ ] 2.3 分公司选择改为下拉（用 branchOptions），传 `fromBranch`/`toBranch` FK
- [ ] 2.4 确保所有空字符串字段不会触发 CharField 验证错误

## 3. 检查移动端

- [ ] 3.1 检查 `MobileAssign.vue` 是否有同样问题并修复
- [ ] 3.2 检查 `MobileTransfer.vue` 是否有同样问题并修复

## 4. 验证

- [ ] 4.1 本地测试领用出库流程
- [ ] 4.2 本地测试调拨流程
- [ ] 4.3 构建并部署到服务器
