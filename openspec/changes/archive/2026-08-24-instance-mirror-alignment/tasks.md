## 1. 修复与命令

- [x] 1.1 check_ledger_consistency：实例聚合与警告去重查询补 `.order_by()`（清默认排序），警告改为一行一品目并携带实例条数
- [x] 1.2 新增 align_ledger_to_instances 命令：默认预览 / --confirm 经唯一写入口出非期初调整单；幂等；branch 为空跳过警告

## 2. 测试与收尾

- [x] 2.1 警告去重用例（同品目 3 实例 → 1 行警告带条数）
- [x] 2.2 对齐命令全流程用例（切换→对账失败→预览不落单→确认落单对账通过→幂等）
- [x] 2.3 pytest 全量回归绿
