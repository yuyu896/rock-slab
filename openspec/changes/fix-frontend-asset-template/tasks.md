## 1. 前端资产模板表头

- [x] 1.1 `utils/importTemplate.ts` 的 `ASSET_HEADERS` 改为指定 15 列：分公司、资产编号、资产类目、物品分类、资产名称、入库日期、是否租用、数量、规格、单价、购入金额、出库日期、所属部门、当前状态、备注

## 2. 验证

- [x] 2.1 前端 `vue-tsc --noEmit` 通过（exit 0）
- [ ] 2.2 本地手动：资产列表下载模板确认恰为 15 列、无多余列
