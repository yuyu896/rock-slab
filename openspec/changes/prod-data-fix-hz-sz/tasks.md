## 1. 纠错命令实现

- [x] 1.1 新建 `backend/apps/assets/management/commands/prod_data_fix_hz_sz.py`：常量固化两案目标，`--apply` 开关，案级 `transaction.atomic`；复用 0914 的 `_transfer_ledger_plan` / `_delete_transfer_with_born_instances`
- [x] 1.2 案1 杭州235：前置断言（无实例、无流转单据 from/to）→ 成对删 81 张调整单 + 81 行台账；节点/授权/字典保留；幂等跳过
- [x] 1.3 案2 20分苏州：生平单收集（行链 ∪ 出生）→ 断言（生效/纯实例行/行链实例全在本分公司）→ 聚合净额回退（断言非负）→ 删关联/实例/单据/通知；发号序列保留
- [x] 1.4 `--apply` 末尾内嵌对账复验，非零差异 exit 1

## 2. 测试

- [x] 2.1 案1 功能：调整单+台账成对清空、他分公司同品目不动、节点与授权保留、对账零差异；边界：存在流转单据/实例时拒绝且零变更
- [x] 2.2 案2 功能：复刻生产形态（2 实例 6 单含退役处置）整清后单/实例/台账归零、对账零差异；边界：行链他分公司实例拒绝且零变更
- [x] 2.3 安全：dry-run 不写库、幂等重跑
- [x] 2.4 后端全量 pytest 通过

## 3. 生产执行

- [x] 3.1 部署新镜像；`/root/backup_db.sh` 即时备份（rock_slab_20260915_150806.sql.gz）
- [x] 3.2 dry-run 存档复核（案1 81 调整单+81 行 总量 8130；案2 六张单号与净额 在库 −1）
- [x] 3.3 `--apply` + 内嵌对账；独立复跑 `check_ledger_consistency` exit=0（3678 行 × 3 列 + 实例镜像零差异）
- [x] 3.4 抽查：杭州235 台账/调整单 0、节点在、李晴晴授权 1 条保留；20分苏州实例 0、A-a00011 在库 0、65 行数量型非零不动；六张生平单零残留；幂等复证两案全跳过
