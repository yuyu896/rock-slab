## 1. 纠错命令实现

- [ ] 1.1 新建 `backend/apps/assets/management/commands/prod_data_fix_20260914.py`：常量固化三案目标（温州二分/潍坊二分→潍坊分公司/CG 两单号），`--apply` 开关，案级 `transaction.atomic`
- [ ] 1.2 案一·温州二分：收集该分公司实例品目出生采购单（断言纯实例行+已入库）→ 删 TransferLineInstance → 删 FixedAsset → 删 Transfer（级联行）→ 按 `_line_plan` 同源矩阵实时计算回退量直减台账（断言非负）；发号序列保留
- [ ] 1.3 案二·潍坊合并：改挂 4 张采购单（to_branch + 单头文本）、75 张调整单、75 行台账（交集相加合并）、员工 branch + ManagementScope 改挂；断言零引用后删 6 条部门字典与组织节点；dry-run 输出合并前后对照表
- [ ] 1.4 案三·测试单：按单号删 CG20250413-001 / CG20250310-001（链接→实例→单据），台账在库回退（实时算量、断言非负），删关联泛化通知
- [ ] 1.5 幂等与安全：目标不存在/已迁移时跳过并报告；`--apply` 末尾内嵌对账复验（调 check_ledger_consistency 同逻辑），非零差异 exit 1

## 2. 测试

- [ ] 2.1 pytest 造数覆盖三案：温州式清空（实例+出生单删除、数量品目不动、镜像归零）；潍坊式合并（同品目相加、员工/授权迁移、节点硬删、对账零差异）；测试单回退（单/实例/台账/通知四净）
- [ ] 2.2 边界测试：前置断言拒绝（单据状态不符/回退为负）、幂等重跑零写入、dry-run 不写库
- [ ] 2.3 后端全量 pytest 通过

## 3. 生产执行

- [ ] 3.1 部署新镜像；执行前 `/root/backup_db.sh` 即时备份
- [ ] 3.2 dry-run 输出存档并人工复核（单号/数量/合并对照与提案一致）
- [ ] 3.3 `--apply` 执行 + 内嵌对账；独立复跑 `check_ledger_consistency` exit=0
- [ ] 3.4 线上抽查：温州二分实例列表空且数量品目库存如旧、组织架构无"潍坊二分"、测试单号详情 404、通知中心无悬空引用
