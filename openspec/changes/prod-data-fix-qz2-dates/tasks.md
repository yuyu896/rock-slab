## 1. 纠错命令实现

- [x] 1.1 新建 `backend/apps/assets/management/commands/prod_data_fix_qz2_dates.py`：常量表 (编号→date)，`--apply` 开关，单事务；仅更新值不同者
- [x] 1.2 前置断言：目标全部存在且属泉州二分；幂等；末尾统一对账复核

## 2. 测试

- [x] 2.1 功能：7 实例日期逐个更新、出生单日期不变、台账/镜像零变化、对账零差异；幂等重跑全跳过
- [x] 2.2 边界：目标缺失中止零变更；dry-run 不写库；后端全量 pytest 通过（721 passed）

## 3. 生产执行（不推送通道）

- [x] 3.1 本地 commit（不 push）；scp 命令文件至服务器
- [x] 3.2 挂载 dry-run 复核（7 条 旧值→新值，-44 号幂等空改）
- [x] 3.3 备份（rock_slab_20260917_171852.sql.gz）→ 挂载 `--apply` + 对账复核（5425 行零差异）
- [x] 3.4 抽查 7 实例新值全部 OK（6 更新+1 空改）、两张出生单日期原样；幂等复证实际更新 0 条

## 4. 回滚（2026-09-18：前案改错对象）

前案将"采购日期"诉求误落到入库日期列（采购日期实为出生单日期的派生值，界面/标签/生平仍显示旧值）。用户决定回滚入库日期、采购日期另行立项（方案 A：个体覆盖字段）。

- [x] 4.1 新增回滚命令 `prod_data_fix_qz2_revert`：7 实例入库日期按快照不变量（=出生单调拨日期）重建，精确还原前案写入；断言全存在/属泉州二分/有出生行
- [x] 4.2 测试 +2（回滚恢复快照/部分缺失中止+幂等）；后端全量 pytest 通过（733 passed）
- [ ] 4.3 备份 → 挂载 dry-run → `--apply` → 抽查 7 实例入库日期=出生单日期、采购日期（出生单派生）原样
