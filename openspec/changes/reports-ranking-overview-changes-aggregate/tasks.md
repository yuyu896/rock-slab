# 报表三修 — 实施任务

## 1. 后端聚合端点

- [x] 1.1 `changes-by-item`：生效明细行按品目×动作聚合（时间范围+scope），行转列输出；测试（多单同品目聚合/时间过滤/权限范围）
- [x] 1.2 全量 pytest

## 2. 前端

- [x] 2.1 排行 computed 按当前指标降序（图表+导出共用）
- [x] 2.2 总览底部状态汇总表（by-status 组装，定高滚动）
- [x] 2.3 变动 tab 换品目聚合表 + api + 导出同步；vitest
- [x] 2.4 build + 全量 vitest

## 3. 验证与收尾

- [x] 3.1 手验：价值降序/总览状态表/变动聚合（时间范围）
- [ ] 3.2 生产部署（待部署）
