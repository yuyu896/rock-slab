## Why

生产导入期数据错误继续清退（2026-09-15 用户报障，前案见归档 prod-data-fix-wzwf-tz）：①19分台州的工作手机（A-a00007，实例管理品目）数据系错误导入，需按"分公司 × 品目"维度清删；②20分苏州的实例档案（159 个）同属错误导入，需整分公司清空重导。两案数据形态已实测核实（镜像零差异、出生采购单纯行、零调整单），完全落在此前已验证的纠错模式内。

## What Changes

- 新增一次性纠错管理命令 `prod_data_fix_20260915`（默认 dry-run，`--apply` 执行，案级事务+对账门禁，复用 0914 命令的三个助手函数）：
  1. **19分台州 × 工作手机（品目维度）**：删除 2 张纯工作手机行已入库采购单（含明细行）→ 138 个出生实例及行-实例关联 → 台账在库 −138 → 关联泛化通知清理。**仅动该分公司该品目**：A-a00007 为全局字典品目，其他分公司 783 行单据引用不受影响。
  2. **20分苏州 × 实例档案（分公司维度）**：删除 2 张纯实例行已入库采购单 → 159 个实例（工作手机 75 / 一体机 84）及关联 → 对应台账归零（工作手机 −75、一体机 −84）→ 通知清理。67 行数量型台账与其余单据、员工（4 人）、实例发号序列保留。
- 执行后 `check_ledger_consistency` 必须零差异（数量流水 + 实例镜像双不变量）。

## Capabilities

### New Capabilities

（无）

### Modified Capabilities

- `prod-data-remediation`: 「实例档案连出生单据整清」要求从分公司维度扩展出**分公司 × 品目维度**变体（品目作用域断言：出生单须纯该品行、他品目台账/实例不动）

## Impact

- **后端**: `backend/apps/assets/management/commands/prod_data_fix_20260915.py`（新增，复用 0914 助手）+ `backend/tests/test_prod_data_fix_20260915.py`
- **生产数据**: 19分台州工作手机台账在库 138→0、实例 138→0、删 2 单；20分苏州实例 159→0、对应两品目台账归零、删 2 单；通知若干条清理
- 不动模型/API/前端；不动其他分公司的 A-a00007 数据
