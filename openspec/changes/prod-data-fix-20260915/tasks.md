## 1. 纠错命令实现

- [x] 1.1 新建 `backend/apps/assets/management/commands/prod_data_fix_20260915.py`：常量固化两案目标（19分台州×A-a00007 工作手机、20分苏州整清），`--apply` 开关，案级 `transaction.atomic`；复用 0914 命令的 `_transfer_ledger_plan` / `_rollback_ledger` / `_delete_transfer_with_born_instances`
- [x] 1.2 案A 品目维度：按出生实例分公司归属反查出生单（断言 purchase+生效+**纯工作手机行**，混行中止）→ 镜像断言 → 删单/实例/关联 → 台账在库实时算量回退 → 通知清理
- [x] 1.3 案B 分公司维度：套用温州模式——出生单断言纯实例行 → 镜像断言 → 整删+回退（工作手机 −75、一体机 −84）→ 数量型台账/员工/发号序列保留
- [x] 1.4 幂等与安全：目标不存在跳过报告；`--apply` 末尾内嵌对账复验，非零差异 exit 1

## 2. 测试

- [x] 2.1 案A 功能：清删后单/实例/台账归零、**他分公司同品目单据与台账零波及**、本分公司他品目不动、对账零差异
- [x] 2.2 案B 功能：整清后实例归零、数量型库存如旧、对账零差异
- [x] 2.3 边界：混行出生单拒绝且零变更、镜像不一致拒绝、幂等重跑、dry-run 不写库
- [x] 2.4 后端全量 pytest 通过（674 passed）

## 3. 生产执行

- [x] 3.1 部署新镜像；`/root/backup_db.sh` 即时备份（rock_slab_20260915_102504.sql.gz）
- [x] 3.2 dry-run 输出存档并人工复核（单号/回退量与调研一致：台州 2 单 −138、苏州 2 单 −75/−84）
- [x] 3.3 `--apply` + 内嵌对账；独立复跑 `check_ledger_consistency` exit=0（2846 行 × 3 列 + 实例镜像零差异）
- [x] 3.4 线上抽查：台州工作手机台账/实例归零、他品目实例 61 个与台账保留；他分公司工作手机 1160 实例零波及；苏州实例归零、数量型 65 行非零保留、员工 4 人无变化；幂等复证两案全跳过
