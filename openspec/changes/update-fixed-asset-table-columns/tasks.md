## 1. 后端 Serializer 扩展父级字段

- [x] 1.1 在 `FixedAssetSerializer` 中增加父级 Asset 只读字段：序号、资产类目、物品分类、是否租用、数量、规格、单价、购入金额、出库日期（通过 `source='asset.字段'` 或 SerializerMethodField）
- [x] 1.2 确认 `FixedAssetSerializer.fields` 包含全部 19 列对应字段（含已有的 asset_name 资产名称、序列号→电脑序列号映射）

## 2. 后端导出

- [x] 2.1 在 `FixedAssetViewSet` 新增 `export_excel` action，参考 `AssetViewSet.export_excel`
- [x] 2.2 导出按 19 列顺序写表头和数据，父级字段从 `instance.asset` 读取，电脑序列号取 `instance.序列号`
- [x] 2.3 查询集使用 `select_related('asset')` 避免性能问题
- [x] 2.4 文件名 `固定资产表_YYYY-MM-DD.xlsx`
- [x] 2.5 注册 export 路由（`fixed-assets/export`）

## 3. 后端导入模板（含只读列底色+批注）

- [x] 3.1 修改 `FixedAssetViewSet.download_template` 表头为 19 列（与表格顺序一致）
- [x] 3.2 表头首行冻结、加粗、自适应列宽
- [x] 3.3 12 个只读列（序号、分公司编号、分公司、资产类目、物品分类、资产名称、是否租用、数量、规格、单价、购入金额、出库日期）数据区域填充浅灰底色（FFF5F5F5）
- [x] 3.4 只读列表头加批注"此列自动继承，无需填写"

## 4. 后端导入解析

- [x] 4.1 修改 `FixedAssetViewSet.import_excel` 按新 19 列位置解析
- [x] 4.2 写入实例字段：电脑序列号→序列号、供应商、入库日期、所属部门、使用人、当前状态
- [x] 4.3 资产编号（列4）作为关联键查找父级 Asset，找不到报错
- [x] 4.4 父级品目字段导入时只读继承（不写入 Asset）
- [x] 4.5 同步实例的分公司、分公司编号从关联 asset 填充

## 5. 前端表格

- [x] 5.1 修改 `FixedAssetList.vue` 表头为 19 列
- [x] 5.2 修改单元格 `<td>` 绑定，父级字段从 `item.序号`/`item.资产类目` 等（后端已平铺到响应）读取，电脑序列号取 `item.序列号`
- [x] 5.3 修改空状态 `colspan` 为 19（或 20 含操作列）
- [x] 5.4 修改搜索框 placeholder 文案（若涉及）

## 6. 前端新增表单

- [x] 6.1 扩展 `createForm` 字段：增加序列号（电脑序列号）、入库日期（若缺）、当前状态选项确认
- [x] 6.2 新增弹窗 `<el-form-item>` 覆盖：资产编号、序列号、供应商、入库日期、所属部门、使用人、当前状态、备注
- [x] 6.3 父级品目字段不在表单中（只读展示）

## 7. 验证

- [ ] 7.1 启动前后端，打开固定资产表，确认 19 列正确显示、父级字段有值
- [ ] 7.2 测试新增实例，确认表单字段、关联品目、继承字段正确
- [ ] 7.3 测试导出，确认 Excel 19 列
- [ ] 7.4 测试导入模板下载（19 列冻结加粗+只读列灰底+批注）和导入解析
- [ ] 7.5 确认 N+1 性能（列表加载无大量额外查询）
