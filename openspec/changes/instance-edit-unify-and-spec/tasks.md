# 实例编辑归一 + 规格 — 实施任务

## 1. 后端

- [x] 1.1 FixedAsset 加 `规格` 个体覆盖列（migration）；item_spec 三级派生（实例→出生行→字典）；batch-update 白名单加 规格
- [x] 1.2 测试：规格三级派生、批量改规格隔离（同行不连带）、白名单含规格；全量 pytest

## 2. 前端

- [x] 2.1 编辑弹窗整合：上下文（内部编号/品目/使用人/部门）+ 序列号/备注/规格/供应商 可编辑 + 图片上传/更换/删除；行操作列收敛为 编辑/生平/打印 三按钮；旧补录/图片弹窗退役
- [x] 2.2 批量操作菜单加「修改规格」（五项）；列表加备注列（colspan 同步）
- [x] 2.3 vitest：编辑弹窗四项提交、操作列三按钮、批量五项、备注列；build + 全量

## 3. 收尾（停本地）

- [ ] 3.1 本地手验全流程；**不部署**——交用户本地验证后自行部署
