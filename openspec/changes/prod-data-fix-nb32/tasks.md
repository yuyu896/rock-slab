## 1. 纠错命令实现

- [x] 1.1 新建 `backend/apps/assets/management/commands/prod_data_fix_nb32.py`：常量固化目标分公司，`--apply` 开关，事务 + 内嵌对账复验
- [x] 1.2 管理方式维度成对删除：非实例品目（quantity/consumable）的台账行 + 调整单同批删；实例品目行/调整单/实例不动
- [x] 1.3 前置断言：无流转单据（from/to）、非实例品目上无实例档案；幂等跳过

## 2. 测试

- [x] 2.1 功能：数量+耗材清空、实例品目台账/调整单/实例原样、对账零差异
- [x] 2.2 边界：存在流转单据拒绝、非实例品目挂实例拒绝，均零变更
- [x] 2.3 安全：dry-run 不写库、幂等重跑；后端全量 pytest 通过（695 passed）

## 3. 生产执行

- [ ] 3.1 部署新镜像；`/root/backup_db.sh` 即时备份
- [ ] 3.2 dry-run 存档复核（34 调整单 + 34 行，在库 −90；实例侧清单为保留面）
- [ ] 3.3 `--apply` + 内嵌对账；独立复跑 `check_ledger_consistency` exit=0
- [ ] 3.4 抽查：数量行 0、187 实例与 3 行实例台账原样；幂等复证
