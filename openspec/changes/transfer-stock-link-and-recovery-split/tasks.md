## 1. 库存联动修复（后端）

- [ ] 1.1 `_sync_asset` 改为按 `资产编号 + branch=from_branch` 查（不区分分公司 → 区分）
- [ ] 1.2 `_sync_assign`：扣减调出分公司数量（`max(0, 数量-调拨数量)`）；状态改「使用中」或「已领完」
- [ ] 1.3 `_sync_transfer`：调出扣减 + 调入增加（存在则累加、不存在则新建）
- [ ] 1.4 测试：领用后数量下降、调拨后两边变化

## 2. 回收独立菜单（前端）

- [ ] 2.1 `SidebarNav.vue`：回收从「资产流转」子项移出，成为一级菜单
- [ ] 2.2 路由保持 `/transfers/recovery`（后端不变）

## 3. 验证

- [ ] 3.1 pytest + vue-tsc
- [ ] 3.2 本地手动：领用/调拨审批后库存变化、回收菜单独立
