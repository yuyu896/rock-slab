# design — 33分宁波第二批点删（nb33 前案直接复用）

## Context

同 nb33 前案形态：多实例行（7/7/22）中各点删 1 台，行账一致（数量==出生实例数）、仅出生链、镜像平衡。

## Decisions

命令逐行派生自 `prod_data_fix_nb33`（仅 TARGET_NUMBERS 常量替换为 -35/-43/-72），断言体系不变：存在/本分公司/在库/生效采购单/行账一致/镜像一致；行数量联动 −1、台账同额直减、发号序列保留空号不回收。

## Migration Plan

push → 部署 → 备份 → dry-run（3 条 行数量 7→6/7→6/22→21、在库 72→69）→ `--apply` → 对账+抽查 → 幂等复证。

## Open Questions

（无）
